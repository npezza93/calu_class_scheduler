- content_for :title do
  Majors

%h3.mdl-display-2.header
  majors

.layout.vertical.center
  %table.mdl-data-table.mdl-js-data-table.mdl-shadow--2dp
    %tbody
      - @majors.each do |major|
        %tr.pointer.table-row{ href: edit_major_path(major) }
          %td.mdl-data-table__cell--non-numeric.center-text
            = major.major

  -# %ul.collapsible.popout{ data: { collapsible: :accordion } }
  -#   - @majors.each do |major|
  -#     %li
  -#       .collapsible-header
  -#         = major.major
  -#       .collapsible-body.layout.vertical.center
  -#         = form_for major do |f|
  -#           = f.text_field :major
  -#         .flex
  -#         = link_to "delete", @major, method: :delete, data: { confirm: 'Are you sure?' }, class: "delete-major"
  -#

= new_fab new_major_path
