- content_for :title, "Transcript"
- content_for :back_url, transcripts_path
- content_for :toolbar do
  = material_icon.file_upload.css_class("upload-transcript")

= render "form", transcript: @transcript

= render "upload"
