$("#transcript_<%= @transcript.id %>").fadeOut();
$("#transcript_<%= @transcript.id %>").remove();

$.ajax({
  url: "<%= new_user_schedule_path(@user) %>",
}).done(function(html) {
  return $("#temp_replace").append(html);
});