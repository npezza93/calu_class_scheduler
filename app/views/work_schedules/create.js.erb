var el = $("#<%= @service.work_schedule.time_id %> a");
<% if @service.saved? %>
  el.attr("data-method", "delete");
  el.data("method", "delete");
  el.attr("href", "<%= url_for(@service.work_schedule) %>");
  el.addClass("mdc-theme--accent-bg");
<% end %>
