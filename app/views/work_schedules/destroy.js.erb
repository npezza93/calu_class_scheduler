var el = $("#<%= @service.work_schedule.time_id %> a");
<% if @service.destroyed? %>
  el.attr("data-method", "post");
  el.data("method", "post");
  el.attr("href", "<%= raw work_schedules_path(@service.work_schedule.path_id) %>");
  el.removeClass("mdc-theme--accent-bg");
<% end %>
