= form_for @category do |f|
  %h3.mdl-display-2.header
    - if @category.new_record?
      New Category
    - else
      Edit Category

  = f.text_field :category, label: 'Category (ex: Electives, Core, etc.)'

  .category-radio
    = f.radio_button(:minor, false, checked: true, label: 'major')
    = f.radio_button(:minor, true, label: 'minor')

  .category-set-logic-text
    Will the student only need to complete one or all of these sections to complete this category?

  .category-radio
    = f.radio_button(:set_and_or_flag, 'or', label: 'one section')
    = f.radio_button(:set_and_or_flag, 'and', label: 'all sections')

  %ul.collapsible{ data: { collapsible: :accordion }, style: 'margin-bottom: 25px;'}
    = f.fields_for :curriculum_category_sets, @category_sets do |builder|
      = render 'curriculum_category_set_fields', f: builder

  = link_to_add_association 'add section', f, :curriculum_category_sets, 'data-association-insertion-node' => '.collapsible', 'data-association-insertion-method' => 'append', style: 'margin-bottom: 35px;color: #66BB6A;'

  = f.submit
